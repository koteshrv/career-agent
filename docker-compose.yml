version: '3.8'

services:
  backend:
    image: ghcr.io/hariharavk/career-agent-backend:main
    ports:
      - "8000:8000"
    volumes:
      # Mount a single data directory for all databases and encryption keys
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      # Auth credentials — change these before exposing the app to the internet!
      - APP_USERNAME=${APP_USERNAME:-admin}
      - APP_PASSWORD=${APP_PASSWORD:-admin}
      - AUTH_SECRET=${AUTH_SECRET:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      # Fallback if not set via the Settings UI — see .env.example
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-}
      # Dead-man's-switch ping for the scheduled scraper — see .env.example
      - HEALTHCHECK_PING_URL=${HEALTHCHECK_PING_URL:-}
      # Tell the app to save the SQLite database in the mapped data folder
      - DATABASE_URL=sqlite:////app/data/jobs.db
    restart: unless-stopped

  frontend:
    image: ghcr.io/hariharavk/career-agent-frontend:main
    ports:
      - "5173:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
